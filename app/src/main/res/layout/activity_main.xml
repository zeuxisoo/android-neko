<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:custom="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/drawerLayout">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <include layout="@layout/include_toolbar" />

            <com.astuetz.PagerSlidingTabStrip
                android:id="@+id/pageIndicatorMainNavigation"
                android:layout_width="fill_parent"
                android:layout_height="48dp"
                android:background="?attr/mainTabBackground"
                android:textColor="?attr/mainTabTextColor"
                custom:pstsShouldExpand="true"
                custom:pstsIndicatorColor="?attr/mainTabIndicator"
                custom:pstsUnderlineColor="?attr/mainTabUnderline" />

            <android.support.v4.view.ViewPager
                android:id="@+id/viewPagerMainNavigation"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />

        </LinearLayout>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:gravity="center"
            android:padding="3dp"
            android:textColor="?attr/mainTabTextColor"
            android:background="?attr/mainTabBackground"
            android:text="network state"
            android:id="@+id/textViewMainActivityNetworkState" />

    </RelativeLayout>

    <include layout="@layout/include_drawer_menu" />

</android.support.v4.widget.DrawerLayout>
